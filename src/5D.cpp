#include <iomanip>
#include <iostream>
#include <tgmath.h>

using namespace std;

int main() {
  bool debug = false;

  double a, v, l, d, w;
  cin >> a >> v >> l >> d >> w;

  w = min(w, v);

  double t = 0.0, v2;

  cout << fixed << setprecision(5) << endl;

  return 0;
}
