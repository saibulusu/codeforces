#include <iostream>
#include <vector>

using namespace std;

int main() {
  int n;
  cin >> n;

  vector<vector<pair<int, int>>> pairs;

  for (int i = 0; i < n; ++i) {
    for (int j = 0; j < n; ++j) {
      int number;
      cin >> number;

      int pow2 = 0, pow5 = 0;
      while (number > 0) {
      }
    }
  }

  return 0;
}
